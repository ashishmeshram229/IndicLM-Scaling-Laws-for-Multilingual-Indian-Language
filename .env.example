# Copy to .env and fill in. Never commit the real .env file.

# Experiment tracking
MLFLOW_TRACKING_URI=
WANDB_API_KEY=
WANDB_PROJECT=indiclm

# Data sources (credentials only; no data is redistributed by this repo)
HF_TOKEN=

# Infrastructure
INDICLM_DEVICE=cpu          # cpu | cuda | mps
INDICLM_NUM_WORKERS=2
INDICLM_LOG_LEVEL=INFO
